[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mcctl"
dynamic = ["version"]   # single source of truth: src/mcctl/__init__.py __version__ (see release.yml)
description = "Remote control & monitoring for a modded Minecraft server over SSH (Arch Linux client)"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "CarborioLand" }]
keywords = ["minecraft", "ssh", "server", "monitoring", "neoforge", "spark"]
dependencies = ["rich>=13.0"]

[project.optional-dependencies]
dev = ["pytest>=7", "ruff>=0.4"]
ai = ["anthropic>=0.69"]

[project.scripts]
lulism = "lulism.cli:main"
mcctl = "lulism.shim:main"          # deprecated bridge, removed at 3.0.0

[project.gui-scripts]
lulism-gui = "lulism.gui:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = { attr = "lulism.__version__" }

[tool.setuptools.package-data]
mcctl = ["units/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: spawns a real local tmux session with a fake server (requires tmux)",
]
addopts = "-q"

[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501"]
