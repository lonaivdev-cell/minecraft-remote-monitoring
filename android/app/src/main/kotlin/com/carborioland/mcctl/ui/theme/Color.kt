package com.carborioland.mcctl.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * A Minecraft-flavoured palette. The hues are pulled from the blocks the game is made of
 * — grass, dirt, stone, diamond, gold, redstone, emerald, lapis — so the UI reads as
 * "Minecraft" without copying any in-game asset.
 */

// Grass — the primary brand colour (the green "start" energy).
val GrassLight = Color(0xFF7FB238)
val Grass = Color(0xFF5DA130)
val GrassDark = Color(0xFF3E6B1F)

// Dirt — warm browns used under the grass and for secondary chrome.
val DirtLight = Color(0xFF9C6E4A)
val Dirt = Color(0xFF79553A)
val DirtDark = Color(0xFF5C4332)

// Stone — the neutral panel/button material, with bevel highlight/shadow shades.
val StoneLight = Color(0xFF9D9D9D)
val Stone = Color(0xFF787878)
val StoneMid = Color(0xFF5A5A5A)
val StoneDark = Color(0xFF3B3B3B)

// Deepslate — the dark backgrounds the app sits on.
val DeepslateBg = Color(0xFF15151A)
val DeepslateSurface = Color(0xFF1F1F26)
val DeepslateSurfaceHi = Color(0xFF2A2A33)
val DeepslateLine = Color(0xFF3A3A45)

// Ores & gems — semantic accents.
val Diamond = Color(0xFF4DD9CE)
val DiamondDark = Color(0xFF2E9E96)
val Emerald = Color(0xFF3FB950)
val GoldXp = Color(0xFFFCC93D)
val GoldXpDark = Color(0xFFC9991F)
val Redstone = Color(0xFFE0473F)
val RedstoneDark = Color(0xFFB02E2E)
val Lapis = Color(0xFF2D6EC4)

// Text — bone white on deepslate, with a dim variant for secondary copy.
val BoneWhite = Color(0xFFECE6DA)
val BoneDim = Color(0xFFA9A296)
val InkOnGrass = Color(0xFF15240B)
