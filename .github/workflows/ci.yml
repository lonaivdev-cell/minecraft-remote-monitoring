name: ci

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: tmux
          version: 1.0

      - name: Install package + dev deps
        run: pip install -e ".[dev]"

      - name: Lint
        run: ruff check src tests

      - name: Unit tests
        run: pytest -m "not integration" -v

      - name: Integration tests (real tmux + fake server)
        # a skipped integration suite is an empty safety net, not a pass: the
        # flag makes a missing tmux (or missing console scripts) fail the job
        # instead of exiting 0 with nothing executed.
        env:
          LULISM_REQUIRE_INTEGRATION: "1"
        run: pytest -m "integration" -v
